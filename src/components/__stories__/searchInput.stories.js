import React from "react";
import Search from "../searchInput";
export default { title: "Search" };
export const SearchTest = () => (
  <Search label="Search Label" value="place holder" onChange={() => {}} />
);
